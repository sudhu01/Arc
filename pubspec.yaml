name: arc
description: "Arc - Bold athletic gym progress tracker"
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.11.1

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8

  # Lightweight state management for the app store.
  provider: ^6.1.2
  sqflite: ^2.4.2+1
  path: ^1.9.1
  flutter_secure_storage: ^10.3.1
  cryptography: ^2.9.0
  crypto: ^3.0.7
  bip39: ^1.0.6
  qr_flutter: ^4.1.0
  mobile_scanner: ^7.2.0
  http: ^1.6.0
  image_picker: ^1.2.2
  permission_handler: ^12.0.2

  # Sharing a workout as a graphic: capture to a temp file, hand it to the
  # system share sheet. `image` does the JPEG encode — Flutter's own
  # `toByteData` only emits PNG or raw RGBA.
  share_plus: ^13.3.0
  path_provider: ^2.1.6
  image: ^4.9.1

  # Hosts the muscle-group body renderer on the Exercises screen. Its assets are
  # bundled and served from an in-process loopback HTTP server (see
  # lib/body/body_server.dart) — nothing is fetched at runtime, so the body
  # renders the same offline as on wifi.
  webview_flutter: ^4.13.0
  flutter_local_notifications: ^22.3.0
  workmanager: ^0.10.7

  # The rest timer (lib/timer/). `timezone` is not a feature — it is what
  # `zonedSchedule` takes, and scheduling an exact alarm is the only way the
  # buzz survives Arc being swapped away or killed.
  #
  # `file_selector` rather than `file_picker` for the custom alarm sound:
  # file_picker pins win32 ^5 and share_plus pins ^6, so the two cannot coexist.
  # file_selector goes through Android's document picker, which also means
  # choosing a sound needs no storage permission at all.
  audioplayers: ^6.7.1
  vibration: ^3.2.0
  timezone: ^0.11.1
  file_selector: ^1.1.0

  # The bubble that floats over other apps. Opt-in: it runs a second Flutter
  # engine behind SYSTEM_ALERT_WINDOW, which the user has to grant by hand.
  flutter_overlay_window: ^0.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0
  sqflite_common_ffi: ^2.4.0+3

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # Bundled variable fonts, shared by both themes:
  #   Sora          — primary: display + body
  #   Space Grotesk — secondary: numerals
  fonts:
    - family: Sora
      fonts:
        - asset: assets/fonts/Sora.ttf
    - family: Space Grotesk
      fonts:
        - asset: assets/fonts/SpaceGrotesk.ttf

  # The body renderer: page, scene, baked mesh, and a bundled three.js (MIT).
  # Each directory is listed separately — Flutter does not recurse into
  # subfolders, so the postprocessing addons silently would not ship otherwise.
  assets:
    - assets/body/
    - assets/body/addons/postprocessing/
    - assets/body/addons/shaders/
    # Arc's default rest alarm. The same bytes also live at
    # android/app/src/main/res/raw/arc_rest.wav, because the notification
    # channel can only reach a raw resource while the app is dead — both
    # copies are written by tool/make_rest_tone.mjs.
    - assets/sound/

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
